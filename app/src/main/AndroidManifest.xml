<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dycode.edu.kuissdapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Kuis SD"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SubjectActivity" />
        <activity android:name=".DialogActivity" />
        <activity android:name=".KelasActivity" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".Edit" />
        <activity android:name=".mat1" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/title_activity_splash"
            android:theme="@style/AppTheme" />
        <activity android:name=".SoaalActivity" />
        <activity android:name=".SkorActivity" />
        <activity android:name=".SkorrActivity"></activity>
    </application>

</manifest>